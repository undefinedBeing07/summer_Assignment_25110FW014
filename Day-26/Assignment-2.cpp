#include<bits/stdc++.h>
using namespace std;
int main(){
    int age;
    cout<<"Enter the age to check for the eligibility: ";
    cin>>age;
    
    if(age<18) cout<<"You are not eligible!!!"<<"\n";
    else cout<<"You are eligible to vote!!"<<"\n";
}
